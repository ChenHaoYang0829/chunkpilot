buildscript {
    configurations.classpath {
        resolutionStrategy.cacheChangingModulesFor 0, 'seconds'
    }
}

plugins {
    id 'dev.architectury.loom' version '1.13.469' apply false
    id 'architectury-plugin' version '3.5.169' apply false
}

ext {
    // 版本常量 —— port/1.21.5: 两个模块统一到 MC 1.21.5
    minecraftVersion = "1.21.5"

    // Fabric (PORT_MATRIX 已核实: loader 0.19.5 / api 0.128.2+1.21.5)
    fabricLoaderVersion = "0.19.5"
    fabricApiVersion = "0.128.2+1.21.5"
    yarnMappings = "1.21.5+build.1"   // 当前未使用 (fabric 模块用 officialMojangMappings)

    // NeoForge —— 21.5.x 是 MC 1.21.5 的对应线, 21.5.98 = PORT_MATRIX 已核实
    neoforgeVersion = "21.5.98"

    // Architectury —— 16.1.4 的 mods.toml 声明 minecraft [1.21.4,) / neoforge [21.0.110-beta,) ⇒ 1.21.5 可用
    architecturyVersion = "16.1.4"
    
    // Mod
    modVersion = "0.10.10-alpha"  // v0.10.6: 发送器 k 默认 0.15->0.6 (近前方优先, 前方集中度 0.410->0.674, mspt 更优)
    modGroup = "com.chunkpilot"
    modId = "chunkpilot"
}

subprojects {
    apply plugin: 'java'
    // 2026-08-01: fabric 用官方 fabric-loom-remap, 不用 architectury loom/plugin
    if (project.name != 'fabric') {
        apply plugin: 'dev.architectury.loom'
        apply plugin: 'architectury-plugin'
    }

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(21)
        }
    }

    if (project.name != 'fabric') {
        loom {
            silentMojangMappingsLicense()
        }
    }

    // 2026-08-01: minecraft dependency 由各子项目自己设 (fabric 1.21.2, neoforge 1.21.1 不同)
    // 不在这里注入, 各子项目 build.gradle 自己声明

    repositories {
        mavenCentral()
        maven { url "https://maven.fabricmc.net/" }
        maven { url "https://maven.architectury.dev/" }
        maven { url "https://repo.papermc.io/repository/maven-public/" }
        maven { url "https://maven.minecraftforge.net/" }
        maven { url "https://maven.parchmentmc.org/" }
    }

    group = modGroup
    version = modVersion

    tasks.withType(JavaCompile).configureEach {
        options.encoding = 'UTF-8'
    }
}